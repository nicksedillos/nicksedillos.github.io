// Pulls content from an "image gallery" object and uses it to populate a "gallery image" modal.
const populateModal = function(){
	$(`div.image_gallery`).html(
		`<img src=`${}
};

$(document).ready(function(){
	image_gallery.forEach($(`$div.image_gallery`)).html) => {
		`<img src=`
	});
	)
})